# # Statsd Server
[[inputs.statsd]]
#   ## Address and port to host UDP listener on
   service_address = "127.0.0.1:8125"
#
#   ## The following configuration options control when telegraf clears it's cache
#   ## of previous values. If set to false, then telegraf will only clear it's
#   ## cache when the daemon is restarted.
#   ## Reset gauges every interval (default=true)
#   delete_gauges = true
#   ## Reset counters every interval (default=true)
#   delete_counters = true
#   ## Reset sets every interval (default=true)
#   delete_sets = true
#   ## Reset timings & histograms every interval (default=true)
#   delete_timings = true
#
#   ## Percentiles to calculate for timing & histogram stats
#   percentiles = [90]
#
#   ## separator to use between elements of a statsd metric
#   metric_separator = "_"
#
#   ## Parses tags in the datadog statsd format
#   ## http://docs.datadoghq.com/guides/dogstatsd/
#   parse_data_dog_tags = false
   parse_data_dog_tags = true
#
#   ## Statsd data translation templates, more info can be read here:
#   ## https://github.com/influxdata/telegraf/blob/master/docs/DATA_FORMATS_INPUT.md#graphite

# this will provide multiple values for each type
#	"bro.dns.query.type.* measurement.measurement.measurement.field*",
    templates = [
	"bro.log.* measurement.measurement.log",
	"bro.ssh.login.* measurement.measurement.field.ok",
	"bro.dns.query.type.* measurement.measurement.measurement.type.type",
	"bro.notice.type.* measurement.measurement.type.type",
	"bro.conn.vlan.* measurement.measurement.vlan.vlan",
	"bro.weird.name.* measurement.measurement.from.name",
	"bro.known-services.service.* measurement.measurement.from.service"
    ]
#
#   ## Number of UDP messages allowed to queue up, once filled,
#   ## the statsd server will start dropping packets
#   allowed_pending_messages = 10000
#
#   ## Number of timing/histogram values to track per-measurement in the
#   ## calculation of percentiles. Raising this limit increases the accuracy
#   ## of percentiles but also increases the memory usage and cpu time.
#   percentile_limit = 1000
